let
    fx=(x)=>Text.FromBinary(
    #binary(List.Skip(List.Transform(List.Reverse({0..31}),each Number.BitwiseAnd(Number.BitwiseShiftRight(x,_),1)+48),each _=48))
    )
in
    List.Transform({0..1000000},fx)
